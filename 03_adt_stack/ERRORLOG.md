## 9/12 - 6:59pm
One error I encountered was running the wrong configuration. I was
about to run main.cpp to test my implementation, but it ran the
07_overloading configuration instead, not giving me the output I wanted.

## 9/12 - 7:21pm
While I was testing, I wanted to print my pop() call so I can see
if it returns the correct error value. I had an issue in my
printing syntax. Turns out, you need "std::endl" to end the line
AND create a new line. I ran my code without it, and the
text ran on and on. I will include this last piece of code 
at all times from now on.